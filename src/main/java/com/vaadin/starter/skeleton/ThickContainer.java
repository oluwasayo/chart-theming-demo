package com.vaadin.starter.skeleton;

import com.vaadin.flow.component.Component;
import com.vaadin.flow.component.Tag;
import com.vaadin.flow.component.dependency.HtmlImport;

// This is not necessary. Just for illustration.
// The desired custom variables can be added to a container directly using the Element API.
@HtmlImport("src/thick-container.html")
@Tag("thick-container")
public class ThickContainer extends Component {  }
